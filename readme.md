# Readme

## Install

* `npm install`

## .env file

* Create a `.env` file.
* Add `VITE_CAMERA_ID` (cameraID), `VITE_SESSION` (session token), `VITE_SERVER_PORT`(server ip:port)

## Run

* `npm run dev`
